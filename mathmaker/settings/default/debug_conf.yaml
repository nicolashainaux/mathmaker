# Don't modify this file.
# If you wish to define a customized debugging configuration,
# you should duplicate it as dev/debug_conf.yaml and edit this copy instead
# (then you can turn any logger to DEBUG level instead of INFO;
# make sure you also set dbg (and raw, if any) logger level to DEBUG too ).
dbg:
    db: INFO
    wording:
        merge_nb_unit_pairs: INFO
        setup_wording_format_of: INFO
        insert_nonbreaking_spaces: INFO
    Item:
        into_str: INFO
        calculate_next_step: INFO
        eq: INFO
    Quotient:
        into_str: INFO
    Fraction:
        calculate_next_step: INFO
        simplification_line: INFO
        replace_striked_out: INFO
        simplified: INFO
    CommutativeOperation:
        evaluate: INFO
        throw_away_the_neutrals: INFO
    Product:
        into_str: INFO
        requires_inner_brackets: INFO
        expand_and_reduce_next_step: INFO
        is_reducible: INFO
        get_factors_list: INFO
        reduce: INFO
    Sum:
        into_str: INFO
        requires_brackets: INFO
        reduce: INFO
        expand_and_reduce_next_step: INFO
        calculate_next_step: INFO
    Polynomial:
        init: INFO
    BinomialIdentity:
        expand: INFO
    Equation:
        solve_next_step: INFO
    Table_UP:
        init: INFO
